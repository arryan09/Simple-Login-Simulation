{
  "name": "simple-login-simulation",
  "version": "1.0.0",
  "description": "Simple login API using hardcoded credentials (No DB).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
